export interface Contact {
    id: any,
    firstName: string,
    lastName: string,
    phoneNumber: number,
    city: string
}

export interface signup {
    name: string;
    email: string;
    password: string
}

export interface login {
    email: string;
    password: string
}

export interface contactus {
    name: string;
    email: string;
    message: string
}

